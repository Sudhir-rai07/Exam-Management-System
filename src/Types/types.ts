export type GradeCardType = {
    name: string
    time: string
    date: string
    grade: number
}

export type ExamCardType = {
    examName: string
    date: string
    difficulty: React.ReactElement
    time: string
    group: number
}
